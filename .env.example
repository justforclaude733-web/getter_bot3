# Leave empty to develop against the built-in mock catalog.
# Once the bot's API is deployed (same Railway service as the bot),
# point this at it - e.g. the domain Railway gives that service.
VITE_API_BASE_URL=https://your-bot.up.railway.app
